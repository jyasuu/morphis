# morphis
🦀 A high-performance, configuration-driven GraphQL CRUD engine written in Rust. Transform your database schema into a production-ready GraphQL API with custom SQL support via simple YAML/JSON configurations.
