listen_addr: "127.0.0.1:9081"
upstream: "http://127.0.0.1:9082"
jwt_secret: "test-secret-key-for-integration-tests"
require_auth: true
header_mappings:
  - claim: sub
    header: X-User-ID
  - claim: tenant_id
    header: X-Tenant-ID
  - claim: role
    header: X-Role
