listen_addr: "0.0.0.0:9080"
upstream: "http://localhost:4000"
jwt_secret: "change-me-to-a-real-secret"
require_auth: true
header_mappings:
  - claim: sub
    header: X-User-ID
  - claim: tenant_id
    header: X-Tenant-ID
